#
# bspwm hotkeys
#
super + alt + Escape
    bspc quit

super + w
    bspc window -c

super + t
    urxvt -cd /home/proycon

super + u
    pcmanfm

super + q
    bspc desktop -B

super + {s,f,S}
    bspc window -t {floating,fullscreen,pseudo_tiled}

<<<<<<< HEAD
super + alt + S
=======
super + alt + s
    bspc window -t sticky

super + ctrl + s
>>>>>>> 0693585... update
    bspc window -t sticky

super + {grave,Tab}
    bspc {window,desktop} -f last

super + apostrophe
    bspc window -s last

super + e
    /home/proycon/bin/emptywindow

super + alt + w
    bspc window -k  

super + z
    /home/proycon/bin/house.sh "office light on"

super + shift + z
    /home/proycon/bin/house.sh "office light off"

super + shift + alt + z
    /home/proycon/bin/house.sh "lights off"

super + x
    /home/proycon/bin/house.sh "tv light on"

super + shift + x
    /home/proycon/bin/house.sh "tv light off"

super + shift + alt + x
    /home/proycon/bin/house.sh "lights off"

super + h
    firefox https://home.anaproy.nl

super + i
    urxvt -cd /home/proycon -T irssi -e /home/proycon/bin/tm irssi 

super + shift + m 
    urxvt -cd /home/proycon -T ncmpcpp -e ncmpcpp

super + m
    urxvt -cd /home/proycon -T mail -e ssh -Y -A -t anaproy.nl /home/proycon/bin/tm_alot

super + v
    urxvt -cd /home/proycon -T vim -e /home/proycon/bin/tm vim

super + {p,n}
    bspc control --record-history off; \
    bspc window {older,newer} -f; \
    bspc control --record-history on

super + y
    bspc window -w last.manual

super + b
    bspc window -s biggest

super + {_,shift + }{h,j,k,l}
    bspc window -{f,s} {left,down,up,right}

super + {_,shift + }c
    bspc window -f {next,prev}

super + {_,shift + }slash
    bspc desktop -C {forward,backward}

super + {_,shift + }backslash
    bspc desktop -F {vertical,horizontal}

super + {comma,period}
    bspc desktop -l {tiled,monocle}

super + bracket{left,right}
    bspc desktop -f {prev,next}

super + alt + {comma,period,slash}
     bspc config -d focused window_gap {5,25,50}

super + ctrl + {h,j,k,l}
    bspc window -p {left,down,up,right}

super + ctrl + {_,shift + }space
    bspc {window -p cancel,desktop -c}

super + alt + {h,j,k,l}
    bspc window -e {left -10,down +10,up -10,right +10}

super + alt + shift + {h,j,k,l}
    bspc window -e {right -10,up +10,down -10,left +10}

super + ctrl + {1-9}
    bspc window -r 0.{1-9}

super + g
    feh --bg-scale /home/proycon/Pictures/Local/wallpapers/03529_marronbells_5120x2880.jpg && compton -cC -z -r 3 -l 2 -t 2 -f -b 

super + alt + g
    xsetroot -solid "#333"  && killall compton

super + {_,shift + }{1-9,0,minus,equal}
    bspc {desktop -f,window -d} ^{1-9,10,11,12}

super + alt + minus
    xrandr --output eDP1 --scale 0.5x0.5

super + alt + equal
    xrandr --output eDP1 --scale 1x1

super + {Left,Right}
    bspc desktop -f {prev,next}

super + {_,shift +} r
    bspc desktop --rotate {90,270}

~button1
    bspc pointer -g focus

super + button{1-3}
    bspc pointer -g {move,resize_side,resize_corner}

super + !button{1-3}
    bspc pointer -t %i %i

super + @button{1-3}
    bspc pointer -u

alt + @F2
    /home/proycon/bin/lala

alt + @F3
    setinput.sh

alt + @F5
    mountssh
    
alt + @F9
    setxkbmap proylatin

alt + @F10
    setxkbmap proyeo

alt + @F11
    setxkbmap proyru

alt + @F12
    setxkbmap proyar
#
# wm independent hotkeys
#
super + Return
    urxvt -cd /home/proycon

super + space
    dmenu_run -fn "Ubuntu Mono-10"


# make sxhkd reload its configuration files:
super + ctrl + Escape
    pkill -USR1 -x sxhkd

super + Escape
    /usr/bin/xscreensaver-command --lock 

super + shift + Escape
    systemctl suspend

XF86Audio{Raise,Lower}Volume
    amixer sset Master {665+,655-} unmute

XF86AudioMute
    amixer -D pulse set Master 1+ toggle
