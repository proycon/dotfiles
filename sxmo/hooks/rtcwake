#!/usr/bin/env sh

#attempts=0
#test if the network is back up and functional
#while [ $attempts -le 7 ]; do
#    fping -c1 -t1000 anaproy.nl > /dev/null 2> /dev/null
#    if [ $? -eq 0 ]; then
#        break
#    fi
#    attempts=$(( attempts + 1 ))
#done
#disconnect after 6 seconds (3 from script, 3 from screenlock's SuspendPending state, 1 leverage)
#~/dotfiles/notifyclient.sh -W 6 >> ~/.notifications.log 2>> ~/.notifications.err

sleep 3 #give it a few second to process notifications
